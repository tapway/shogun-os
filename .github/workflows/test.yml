name: Tests

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    name: Run test suite
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip
          cache-dependency-path: shogun-web/server/requirements.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip --no-input
          pip install -r shogun-web/server/requirements.txt --no-input
          pip install pytest pyyaml --no-input

      - name: Run tests
        run: bash scripts/run-tests.sh
